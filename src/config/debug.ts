/**
 * Debug flags — flip these for testing.
 * Set UNLOCK_ALL_LEVELS to true to bypass star requirements.
 */
export const DEBUG = {
  UNLOCK_ALL_LEVELS: true, // ← set false for production
} as const;
